/** Port of utils/notificationArticleLink.ts:articlePath — kept in sync with the client route. */
export function articlePath(articleId: string): `/article/${string}` {
  return `/article/${articleId}`;
}
