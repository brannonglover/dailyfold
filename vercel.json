{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "echo 'ERROR: Set Vercel Root Directory to backend (see README Deploy backend). Repo root is Expo-only — no Next.js here.' && exit 1"
}
